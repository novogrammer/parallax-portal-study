# Parallax Portal Study

## 目的

- パララックスポータルを特定の演出やシーンに依存しない形で一般化する。
- 現段階ではソースコードを実装せず、用語、座標系、数式、責務、検証条件をMarkdownで整理する。
- 将来はVite + TypeScriptで実行可能なWebプロジェクトへ発展させる。

## 現段階の進め方

- 実装開始をユーザーが明示するまでは、アプリケーションコードやビルド設定を作成しない。
- 設計では「不変の幾何アルゴリズム」「演出ポリシー」「シーン契約」「描画ランタイム」を分離する。
- 数式には変数の意味、単位、座標系、前提条件を併記する。
- 判断できない仕様を既成事実にせず、仮定または未決事項として記録する。

## reference

- `reference/` 配下は別リポジトリへの参照用symlinkであり、読み取り専用として扱う。
- 参照先のファイルを編集、移動、削除しない。
- 既存実装は挙動を理解する材料とし、その構造をそのまま新設計の制約にはしない。

## 将来のWeb実装

- Vite + TypeScriptを基本構成とする。
- 幾何計算はDOM、Three.js、描画ループから独立した純粋なモジュールにする。
- 固定値やシーン固有値をアルゴリズムへ埋め込まず、明示的な設定として渡す。
- UI、ポータル配置、3Dシーン、描画制御の責務を分離する。
- 依存パッケージは必要最小限にし、追加理由を説明できる状態にする。
- 実装後は少なくとも型検査と本番ビルドを通す。

## 変更方針

- 変更は依頼範囲に限定し、小さく確認可能な単位で行う。
- `node_modules/`、`dist/`、一時生成物は管理対象に含めない。
- 設計と実装が食い違った場合は、どちらを正とするか確認してから更新する。
