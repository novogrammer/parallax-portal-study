# Parallax Portal Study

## 目的

- パララックスポータルを特定の演出やシーンに依存しない形で一般化する。
- Vite + TypeScript + Three.jsによる実装とMarkdownの設計文書を対応させ、投影モデル、責務、検証条件を明確に保つ。
- WebGL基準実装を比較基準とし、将来のWebGPU移行を検証できる状態にする。

## 現在地

- 第1段階のWebGL基準実装は完了している。
- 第2段階のWebGPU移行検証は未着手であり、開始時期と対象範囲はユーザーと確認する。
- 現在地と段階ごとの対象範囲は `docs/implementation-plan.md` を正本とする。

## 設計方針

- 設計では「不変の幾何アルゴリズム」「演出ポリシー」「シーン契約」「描画ランタイム」を分離する。
- 数式には変数の意味、単位、座標系、前提条件を併記する。
- 幾何計算はDOM、Three.js、描画ループから独立した純粋なモジュールにする。
- 固定値やシーン固有値をアルゴリズムへ埋め込まず、明示的な設定として渡す。
- UI、Portal配置、3D Scene、描画制御の責務を分離する。
- PortalのCSS高とSceneのCamera移動高はデザイン入力とし、Portal間の縮尺統一をシステム要件にしない。
- 判断できない仕様を既成事実にせず、仮定または未決事項として記録する。

## ドキュメント方針

- ドキュメントは、現時点で有効な設計を過不足なく簡潔に記述する。
- 検討過程や会話の時系列を本文へ残さず、確定事項、仮定、未決事項を区別する。
- 同じ概念や数式を複数箇所で説明せず、正本となる記述を1箇所にまとめる。
- 古くなった説明や現在の設計と矛盾する記述は、追記で打ち消さず整理または置き換える。
- 文書は責務が明確に分かれる場合に分割し、細かすぎる文書を増やさない。
- 概念と責務は `docs/overview.md`、投影の数式は `docs/vertical-projection.md`、合格条件と実装値は `docs/validation.md` を正本とする。

## reference

- `reference/` 配下は別リポジトリへの参照用symlinkであり、読み取り専用として扱う。
- 参照用symlinkは `reference/prototype` として扱う。
- 参照元の固有プロジェクト名を、このリポジトリの文書、コード、設定、ファイル名へ持ち込まない。
- 参照先のファイルを編集、移動、削除しない。
- 既存実装は挙動を理解する材料とし、その構造をそのまま新設計の制約にはしない。

## Web実装

- Vite + TypeScriptを基本構成とする。
- 依存パッケージは必要最小限にし、追加理由を説明できる状態にする。
- コード変更後は `npm test` と `npm run build` を実行する。
- 見た目に影響する変更では、必要に応じてChromeでPCとSPの表示、スクロール、Console errorを確認する。

## 変更方針

- 変更は依頼範囲に限定し、小さく確認可能な単位で行う。
- `node_modules/`、`dist/`、一時生成物は管理対象に含めない。
- 設計と実装が食い違った場合は、どちらを正とするか確認してから更新する。
